import ColorPicker from '../components/tools/ColorPicker';

describe('FrameList.prototype.render', () => {
  it('Should be an instance of function', () => {
    expect(ColorPicker.prototype.startCanvas).toBeInstanceOf(Function);
  });
});
