import colorPixel from '../utils/colorPixel';

describe('Save.prototype.init', () => {
  it('Should be an instance of function', () => {
    expect(colorPixel.colorPixel).toBeInstanceOf(Function);
  });
});
