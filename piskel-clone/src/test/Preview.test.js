import Preview from '../screens/export/save/Save';

describe('Save.prototype.init', () => {
  it('Should be an instance of function', () => {
    expect(Preview.prototype.init).toBeInstanceOf(Function);
  });
});
